name: flutter_video_status
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_ffmpeg: ^0.3.0
  file_picker: ^1.13.3
  video_player: ^0.10.10
  video_player_platform_interface: ^2.1.0
  path_provider: ^1.6.14

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true